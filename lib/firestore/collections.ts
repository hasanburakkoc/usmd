export const COLLECTION_TREATMENT_CATEGORIES = "treatment_categories";

/** `POST /api/consultations` — one document per form submit (Admin SDK only). */
export const COLLECTION_FORM_SUBMISSIONS = "consultation_form_submissions";
