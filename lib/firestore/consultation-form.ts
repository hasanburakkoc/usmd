/**
 * Workflow for `consultation_form_submissions` (see `COLLECTION_FORM_SUBMISSIONS`).
 * Server-only; safe to import from API routes.
 */
export type ConsultationFormStatus = "new" | "in_review" | "closed";
